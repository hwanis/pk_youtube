	
function Login(form) {
    alert('a');
	/*
        if (form.emp_id.value == ""){
                alert(" 아이디를 입력 해주세요.");
                form.emp_id.focus();
                return;
        }

        if (form.passwd.value == ""){
                alert("비밀번호를 입력 해주세요.");
                form.passwd.focus();
                return;
        }

        form.submit();
        */
}